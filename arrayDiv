import numpy as np
c = [5,8,6,1,10]
g = []
def arr(elem):
    
    index = 0

    while index < len(c):
        result = c[elem] / c[index]
        index +=1
        g.append(result)
    return g
a=0
while a in range(len(c)):
    a+=1
    print(np.array(arr(elem = a-1)))
